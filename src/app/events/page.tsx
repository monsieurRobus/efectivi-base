import { EventsView } from '@/components/organism/EventsView'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <main>
        <EventsView />
    </main>
  )
}

export default page